# September 16 continuation handoff

This handoff is being prepared while the final milestone runs. **The 69-module
cold certification is not yet accepted.** Check the final outcome below and
`campaign.json` before resuming.

## User direction

Stop this session after the 69 modules compile and certify together, then hand
off. Do not expand coverage in this session. Future implementation is delegated
to Luna at high reasoning; the primary coordinates, reviews source diffs and
compact result files, and commits. Never read implementer transcripts. Agents
share a checkout and need narrow file ownership. Use `followup_task` to wake a
completed agent; `send_message` alone does not wake it.

## Completed changes and evidence

The replay fixes are published in runtime commit
`748a80a8d3cf57e370129c7f5c36a976b7c96249`. Relation's three explicit proof
overrides are committed at `7ac69cf`; Function's one-line `extend_apply` proof
is committed at `adb0497`. The production override database has 21 entries in
17 modules. Original tactics remain comments. No acceptance checks were weakened.

The original foundation retries pass isolated Linux module/declaration checks:
IsEmpty.Basic (17 replayed calls), Nat.Init (4 replayed + 1 explicit), Relation
(4 replayed + 3 explicit). These are separate from whole-tree acceptance.

The selected IsEmpty.Basic → Data.Option.Basic dependency cone contains 69
modules: 7 target modules with 91 canonical calls, and 62 zero-call prerequisites.
All seven target modules pass individual checks: 90 replayed calls and one
explicit Function proof, with zero unobserved calls. The targets are Logic.Basic,
Logic.ExistsUnique, Logic.Function.Basic, Logic.Function.Defs, Logic.IsEmpty.Basic,
Logic.Nontrivial.Defs, and Data.Option.Basic.

Logic.Basic needed a 128 GiB virtual-address limit and peaked at 68.3 GB Lean
RSS. Require at least 96 GiB available before a high-memory job and run only one
such experiment at once. Keep the existing 12 GiB disk/memory reserves.
Function.Basic's unpaired-fresh-constant failure persisted at the larger limit;
the reviewed override replaces only `simp only [extend_def, dif_pos,
exists_apply_eq_apply]` with `rw [extend_def, dif_pos (exists_apply_eq_apply f a)]`.
Its canonical declaration oracle passed with one stock-only private proof under
the unchanged oracle policy; it is not byte-identical declaration output.

The archived 529 successful reports / 2,063 calls and 62 failures remain
historical. Whole-Mathlib acceptance remains zero.

## Reproducible cone inputs

Local artifact root:
`/Users/ptsier/projects/explicit-lean/.lake/foundation-batch-20260916T063022Z-5be4dad2671e1e297934369fb035178a`.
This ignored directory is not available in a fresh clone; preserve its evidence
archive before relying on a portable handoff.

Remote cone root:
`/var/tmp/foundation-batch-20260916T072000Z-cone-firstphase-v3`.
Its `source` checkout is the published 748a80a runtime. Do not rebuild or
reinterpret receipts merely because proof/tracker commits are newer.

| Artifact | SHA-256 |
| --- | --- |
| Fresh 69-module manifest | `7eb30989630cc5b71981b904917fa8ae8105a28608f7ba518bb89e1b31604d39` |
| `dependency-map-69-v2.json` | `dea23e7c66625b85be5937c20410e7913b5fa6f68c4276697d001f033d9f7c0a` |
| `external-edges-69-v2.json` | `e406421b524e84722ded42cb20a21b590c135b60e7eda1cac848292cb3e38731` |
| `dependency-source-join-69-v2.json` | `70392d55e0421d41b8ab20b4e626ee11b3a0a6ee9ff82cfff787f00a678c576f` |
| `target-report-selection-69-v2.json` | `fb210ed6be086f26bef181ff7fdcc71099d615665f3186d419c98b61f005ae19` |
| Reviewed `cone-coldphase.sh` | `55edb8952897f031f0eb0f459257cc15fab9ca3aefa8ba3ed0b1f98d8e91cb29` |

The 260 dependency edges partition into 132 internal and 128 external edges.
All 69 modules must be cold-certified, including the zero-call prerequisites.
Publish into the translated import root, prohibit stock Mathlib fallback, and
audit all 69 resolutions after publication.

Each selected module report authenticates against its own manifest. Its complete
canonical module row must equal the corresponding fresh-69 row; runtime,
implementation, Lean and Mathlib identities must agree. Function alone uses the
reviewed candidate override database. Its 25 canonical occurrences partition
into one manual occurrence (`acca7bbba4fc669d`) and 24 ordinary occurrences.
Those ordinary occurrences have effective IDs after the edit: use the report's
validated canonical-to-effective map, not a direct canonical-ID comparison.
Never edit frozen reports or merge manifests to manufacture agreement.

## Final milestone outcome

Pending. Initial wrapper preflight failed before Python validation because the
native `simpEngineCommandAudit` executable was absent. All seven selected
report/manifest/source paths existed and hash-matched. A narrow build of that
target in the existing Linux runtime is authorized; no cold certificate has
yet been claimed.

## AWS obligations

Only the existing account 538639825139 / us-west-1 host is authorized:
`i-05bfce721923eb190`, r7i.4xlarge, profile `explicit-lean-pilot`. No alternative
profile, second host, second campaign worker, or implicit explicit-lean-cloud.
Never buy or redeem credits. The $20 all-in, 12-hour host and 10-hour worker
bounds remain in force. The immutable absolute cutoff is
**2026-09-16T13:30:00Z**; do not extend it.

Run: `20260916T014201Z-a38ca0ed7740f01e72adebc45720898a`.
Sole campaign worker: SSM `64b1d21b-111f-4231-938f-0ed933663d58`, started
01:48:02Z. Its main checkout `/usr/bin/explicit-lean` remains untouched at
`5145cfbff9ba1b2e99648c04691f8b92265f41a3`. Focused experiments do not constitute
another campaign worker. Preserve evidence before termination and verify exact
resource cleanup. Record fresh worker/host status below before final handoff.

## Next agent

Read this file, `AGENTS.md`, `campaign.json`, and the final raw certificate before
planning more work. Fresh usage telemetry must permit ordinary work before any
costly dispatch. Resume only within unexpired authorization; a new host or
extended campaign needs new user authorization. Remaining project work includes
expanding dependency-ordered coverage and whole-tree acceptance, including zero
remaining executable target calls, preserved original comments, declaration and
computational semantics, import closure, and final trust checks. A passing cone
does not authenticate the global campaign manifest or prove whole-tree success.
