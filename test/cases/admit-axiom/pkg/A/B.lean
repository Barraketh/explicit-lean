-- An axiom declaration. G5 admits `axiom` in generated output only when it
-- corresponds to a source axiom, but v0 does not accept the source command.
set_option autoImplicit false

axiom unproven : 1 = 1
